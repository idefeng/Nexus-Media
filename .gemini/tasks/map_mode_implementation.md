# 任务完成报告：实现地图模式 (Map Mode)

## 状态: 已完成 ✅

### 已实现的子任务:
1. **元数据提取升级**
    - [x] 安装 `exiftool-vendored` 依赖。
    - [x] 在 `electron/exif.ts` 中集成 ExifTool，支持图片和视频坐标提取。
    - [x] 确保坐标自动转换为十进制度数。

2. **数据库结构优化**
    - [x] 在 `database.ts` 中新增 `latitude` 和 `longitude` 列。
    - [x] 实现数据库自动迁移与旧数据一键补全。
    - [x] 添加 `searchMediaByBounds` 空间查询接口。

3. **地图视图开发**
    - [x] 实现 `MapDashboard` 全屏视图。
    - [x] 集成标记聚合功能，提升万级照片下的交互性能。
    - [x] 实现基于地图视野的“空间筛选”模式。

4. **UI/UX 细节提升**
    - [x] 在媒体详情侧边栏嵌入小型交互式地图。
    - [x] 侧边栏导航增加“足迹地图”入口。
    - [x] 完善中英文翻译支持。

### 遇到的挑战与解决方案:
- **NPM 安装问题**: `npm` 环境存在兼容性报错，已改用 `pnpm` 成功安装 Leaflet 及相关依赖。
- **Leaflet 图标丢失**: 通过手动覆盖 `L.Icon.Default` 选项解决了 Vite 环境下资源路径引用的问题。
- **数据一致性**: 实现一套自动补全逻辑，确保老用户在未重新扫描的情况下也能立刻看到地图数据。

### 下一步建议:
- 针对大量照片的情况，可以进一步优化 `getMediaWithLocation` 的返回字段，减小 IPC 通信负载。
- 建议增加“足迹回访”功能，按时间轴在地图上连线展示旅行轨迹。
